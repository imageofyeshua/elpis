<%= form_with(model: @micropost) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="flex flex-col">
    <%= f.text_area :content, placeholder: "Compose new micropost...", class: "w-1/2 rounded-md border border-slate-300 px-3 py-2" %>
    <%= f.submit "Post", class: "w-[100] mt-3 px-4 py-2 bg-blue-600 hover:bg-blue-700 active:bg-blue-800 text-white" %>
  </div>
<% end %>
