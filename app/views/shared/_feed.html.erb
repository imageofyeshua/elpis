<% if @feed_items.any? %>
  <%= render partial: "shared/pagination", locals: { pagination: @pagination } %>
  <ol class="text-blue-900">
    <%= render @feed_items %>
  </ol>
  <%= render partial: "shared/pagination", locals: { pagination: @pagination } %>
<% end %>
